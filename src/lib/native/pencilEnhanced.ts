/**
 * Client-only entry for the Capacitor `PencilEnhanced` plugin. Import only from `"use client"` modules.
 */
export { PencilEnhanced } from "pencil-enhanced";
export type { PencilAvailability, PencilDoubleTapPayload, PencilSqueezePayload } from "pencil-enhanced";
